/**
Checks that "undefined" allocation of scalar fields is OK. */

int main()
{
  init_grid (1);
  {
    interpreter_verbosity (2);
    scalar a[];
    if (b) {
      scalar c[];
    }
    {
      interpreter_verbosity (4);
      datasize;
    }
    {
      interpreter_verbosity (2);
      scalar d[];
      d.i;
    }
  }
}
